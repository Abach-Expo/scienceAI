# Science AI Assistant

## 🔬 Научный ИИ-Ассистент для написания диссертаций и дипломных работ

Полнофункциональное приложение на React Native + Node.js/TypeScript для автоматизации написания научных работ с использованием GPT API.

![App Preview](docs/preview.png)

## ✨ Возможности

### 🤖 ИИ-функции
- **Генерация плана/структуры** - автоматическое создание детального плана научной работы
- **Генерация тезисов и аргументов** - формирование основных тезисов с цепочкой рассуждений (chain-of-thought)
- **Автоматическое написание черновика** - генерация текста по разделам
- **Анализ документа** - проверка логики, фактов, грамматики и стиля
- **Улучшение стиля** - автоматическое редактирование текста
- **Self-review** - самопроверка с использованием chain-of-thought reasoning

### 📚 Поиск научных статей
- Интеграция с **ArXiv API**
- Интеграция с **Semantic Scholar API**
- Сохранение ссылок в проект
- Автоматическое цитирование

### 📄 Экспорт
- Генерация **PDF** через pdf-lib
- Генерация **DOCX** через docx
- Экспорт всего проекта с референсами

### 📊 Аналитика
- Подсчёт слов и символов
- Отслеживание прогресса
- Визуализация статистики

## 🎨 Дизайн

Интерфейс вдохновлён **Cosmos.so** — минималистичный тёмный дизайн с:
- Плавными анимациями переходов (React Native Reanimated)
- Gradient карточками и кнопками
- Glass-morphism эффектами
- Современной типографикой

## 🛠 Технологический стек

### Backend
- **Node.js + Express** - сервер
- **TypeScript** - типизация
- **Prisma** - ORM для PostgreSQL
- **OpenAI API** - интеграция с GPT
- **pdf-lib** - генерация PDF
- **docx** - генерация DOCX

### Mobile (React Native)
- **Expo** - платформа разработки
- **React Navigation** - навигация
- **React Native Reanimated** - анимации
- **Zustand** - state management
- **Expo Linear Gradient** - градиенты
- **Victory Native** - графики

## 📁 Структура проекта

```
science-ai-assistant/
├── backend/
│   ├── prisma/
│   │   └── schema.prisma          # Схема базы данных
│   ├── src/
│   │   ├── index.ts               # Точка входа
│   │   ├── middleware/
│   │   │   └── auth.middleware.ts # JWT аутентификация
│   │   ├── routes/
│   │   │   ├── auth.routes.ts     # Регистрация/вход
│   │   │   ├── project.routes.ts  # CRUD проектов
│   │   │   ├── document.routes.ts # CRUD документов
│   │   │   ├── ai.routes.ts       # ИИ-эндпоинты
│   │   │   ├── search.routes.ts   # Поиск статей
│   │   │   └── export.routes.ts   # Экспорт PDF/DOCX
│   │   ├── services/
│   │   │   └── ai.service.ts      # OpenAI интеграция
│   │   └── utils/
│   │       └── logger.ts          # Winston логгер
│   ├── package.json
│   ├── tsconfig.json
│   └── .env.example
│
├── mobile/
│   ├── src/
│   │   ├── components/
│   │   │   └── ui/
│   │   │       ├── Button.tsx     # Анимированная кнопка
│   │   │       ├── Card.tsx       # Карточки с анимацией
│   │   │       ├── Input.tsx      # Анимированный инпут
│   │   │       ├── Modal.tsx      # Модальные окна
│   │   │       └── Analytics.tsx  # Компоненты статистики
│   │   ├── screens/
│   │   │   ├── HomeScreen.tsx     # Главный экран
│   │   │   ├── ProjectScreen.tsx  # Экран проекта
│   │   │   ├── EditorScreen.tsx   # Редактор документа
│   │   │   ├── SearchScreen.tsx   # Поиск статей
│   │   │   └── AuthScreen.tsx     # Авторизация
│   │   ├── navigation/
│   │   │   └── RootNavigator.tsx  # Навигация
│   │   ├── store/
│   │   │   ├── authStore.ts       # Состояние авторизации
│   │   │   └── projectStore.ts    # Состояние проектов
│   │   ├── services/
│   │   │   └── api.ts             # API клиент
│   │   └── theme/
│   │       ├── index.ts           # Тема Cosmos-style
│   │       └── styles.ts          # Общие стили
│   ├── App.tsx                    # Корневой компонент
│   ├── app.json
│   └── package.json
│
└── README.md
```

## 🚀 Установка и запуск

### Требования
- Node.js >= 18
- PostgreSQL
- OpenAI API ключ

### Backend

```bash
cd backend

# Установка зависимостей
npm install

# Настройка переменных окружения
cp .env.example .env
# Отредактируйте .env файл

# Генерация Prisma клиента
npm run prisma:generate

# Миграция базы данных
npm run prisma:migrate

# Запуск в режиме разработки
npm run dev
```

### Mobile

```bash
cd mobile

# Установка зависимостей
npm install

# Запуск Expo
npm start

# Или для конкретной платформы
npm run ios
npm run android
```

## ⚙️ Конфигурация

### Переменные окружения (backend/.env)

```env
# Server
PORT=3001
NODE_ENV=development

# Database
DATABASE_URL="postgresql://user:password@localhost:5432/science_ai_db"

# OpenAI
OPENAI_API_KEY=your_openai_api_key
OPENAI_MODEL=gpt-4-turbo-preview

# JWT
JWT_SECRET=your_jwt_secret
JWT_EXPIRES_IN=7d

# External APIs
SEMANTIC_SCHOLAR_API_KEY=your_semantic_scholar_key
```

### Mobile API URL

Создайте `.env` в папке mobile:
```env
EXPO_PUBLIC_API_URL=http://localhost:3001/api
```

## 📖 API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/auth/me` - Текущий пользователь

### Проекты
- `GET /api/projects` - Список проектов
- `POST /api/projects` - Создать проект
- `GET /api/projects/:id` - Получить проект
- `PUT /api/projects/:id` - Обновить проект
- `DELETE /api/projects/:id` - Удалить проект

### Документы
- `GET /api/documents/project/:projectId` - Документы проекта
- `POST /api/documents` - Создать документ
- `GET /api/documents/:id` - Получить документ
- `PUT /api/documents/:id` - Обновить документ
- `GET /api/documents/:id/history` - История версий

### ИИ
- `POST /api/ai/generate-outline` - Генерация плана
- `POST /api/ai/generate-arguments` - Генерация тезисов
- `POST /api/ai/generate-draft` - Генерация черновика
- `POST /api/ai/analyze` - Анализ документа
- `POST /api/ai/improve-style` - Улучшение стиля
- `POST /api/ai/self-review` - Self-review

### Поиск
- `GET /api/search/arxiv` - Поиск в ArXiv
- `GET /api/search/semantic-scholar` - Поиск в Semantic Scholar
- `GET /api/search/combined` - Комбинированный поиск
- `POST /api/search/save-reference` - Сохранить ссылку

### Экспорт
- `POST /api/export/pdf` - Экспорт в PDF
- `POST /api/export/docx` - Экспорт в DOCX
- `POST /api/export/project/:id` - Экспорт всего проекта

## 🎯 Пример использования

1. **Регистрация** - создайте аккаунт в приложении
2. **Новый проект** - введите тему диссертации/диплома
3. **Генерация плана** - ИИ создаст структуру работы
4. **Поиск статей** - найдите релевантные научные статьи
5. **Генерация черновика** - ИИ напишет текст по разделам
6. **Редактирование** - внесите свои правки
7. **Анализ** - проверьте логику, факты и грамматику
8. **Улучшение стиля** - ИИ отредактирует текст
9. **Экспорт** - скачайте готовую работу в PDF/DOCX

## 📝 Лицензия

MIT License

## 🤝 Вклад в проект

Pull requests приветствуются! Для крупных изменений сначала откройте issue для обсуждения.

---

Разработано с ❤️ для научного сообщества
